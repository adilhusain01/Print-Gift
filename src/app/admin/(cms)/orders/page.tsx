import { OrderStatus } from "@/components/order-status";
import { getOrders } from "@/lib/data";
import { formatPrice } from "@/lib/format";

export default async function OrdersPage() {
  const orders = await getOrders();
  return <><div><span className="eyebrow">Fulfillment</span><h1 className="mt-4 font-heading text-5xl tracking-[-0.04em]">Orders</h1><p className="mt-2 text-muted-foreground">Review customer details and move work through the queue.</p></div><div className="mt-8 grid gap-4">{orders.map((order) => <article key={order.orderNumber} className="surface p-5 sm:p-6"><div className="flex flex-wrap justify-between gap-4"><div><h2 className="font-heading text-2xl">{order.orderNumber}</h2><p className="mt-1 text-xs text-muted-foreground">{new Date(order.createdAt).toLocaleString("en-IN")}</p></div><OrderStatus orderNumber={order.orderNumber} status={order.status} /></div><div className="mt-5 grid gap-6 border-t border-border pt-5 md:grid-cols-3"><div><p className="text-[0.62rem] font-semibold uppercase tracking-[0.18em] text-muted-foreground">Customer</p><p className="mt-3 font-semibold">{order.customer.name}</p><p className="mt-1 text-sm">{order.customer.phone}</p><p className="mt-2 text-sm leading-6 text-muted-foreground">{order.customer.address}, {order.customer.city} - {order.customer.postalCode}</p></div><div><p className="text-[0.62rem] font-semibold uppercase tracking-[0.18em] text-muted-foreground">Items</p><div className="mt-3 grid gap-2 text-sm">{order.items.map((item) => <p key={item.slug}>{item.quantity} × {item.name}</p>)}</div></div><div><p className="text-[0.62rem] font-semibold uppercase tracking-[0.18em] text-muted-foreground">Total</p><p className="mt-3 font-heading text-3xl">{formatPrice(order.total)}</p>{order.customer.notes ? <p className="mt-3 text-sm leading-6 text-muted-foreground">Notes: {order.customer.notes}</p> : null}</div></div></article>)}</div></>;
}
